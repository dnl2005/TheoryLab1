<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Операции"
        Height="450"
        Width="700">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <!-- Левая колонка - операции и список товаров -->
        <Grid Grid.Column="0" Grid.RowSpan="2"
              Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="150" />
            </Grid.RowDefinitions>

            <!-- Заголовок списка операций -->
            <Label Content="Операции:" 
                   FontWeight="Bold"
                   Margin="0,0,0,5" />

            <!-- Список операций -->
            <ListBox x:Name="OperationList"
                     Grid.Row="1"
                     BorderBrush="Gray"
                     BorderThickness="1"
                     SelectionChanged="OperationList_SelectionChanged"
                     SelectedIndex="0" Margin="0,0,0,77">
                <ListBoxItem Content="Добавить новый товар" />
                <ListBoxItem Content="Отгрузить товар со склада" />
                <ListBoxItem Content="Переместить товар между складами" />
            </ListBox>

            <!-- Заголовок списка товаров -->
            <Label Grid.Row="1"
                   Content="Товары на складе:" 
                   FontWeight="Bold"
                   Margin="0,156,0,46" />

            <!-- Список товаров - ОТОБРАЖАЕТ ДАННЫЕ СКЛАДА -->
            <ListBox x:Name="GoodsList"
                     Grid.Row="1"
                     BorderBrush="Gray"
                     BorderThickness="1"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="0,192,0,0" Grid.RowSpan="3">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="2" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- Правая колонка - управление операциями -->
        <StackPanel Grid.Column="1" Grid.Row="0"
                    Margin="10">

            <Label Content="Текст операции"
                   Margin="0,0,0,0" />

            <TextBox Name="OperationText"
                     Margin="0,0,0,10"
                     Height="30"
                     TextChanged="OperationText_TextChanged" />
            <Button Content="Выполнить"
                    Height="30"
                    Margin="0,0,0,10"
                    Click="Button_Click" />

            <Button Content="Показать контракт"
                    Height="30"
                    Name="ShowContractButton"
                    Click="ShowContractButton_Click" />

            <!-- Индикаторы Pre и Post условий -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Grid.Column="0"
                               Width="15"
                               Height="15"
                               Fill="Red"
                               Name="PreConditionIndicator" />

                    <Label Grid.Column="1"
                           Content="Pre-условие" />
                </Grid>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Grid.Column="0"
                               Width="15"
                               Height="15"
                               Fill="Red"
                               Name="PostConditionIndicator" />

                    <Label Grid.Column="1"
                           Content="Post-условие" />
                </Grid>
            </Grid>

        </StackPanel>

        <!-- Область с подсказками по формату ввода -->
        <Border Grid.Column="1"
                Margin="10,233,10,10"
                BorderBrush="LightGray"
                BorderThickness="1"
                Background="WhiteSmoke" Grid.RowSpan="2">
            <StackPanel Margin="5">
                <TextBlock Text="Формат ввода:" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="&quot;Название товара&quot;, количество" 
                           Margin="0,0,0,10"/>
                <TextBlock Text="Пример:" 
                           FontWeight="Bold"
                           Margin="0,0,0,5"/>
                <TextBlock Text="&quot;Ноутбук&quot;, 5"/>
                <TextBlock Text="&quot;Мышь&quot;, 10"/>
            </StackPanel>
        </Border>

    </Grid>
</Window>