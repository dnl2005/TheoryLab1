<Window x:Class="UI.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Справка"
        ResizeMode="NoResize"
        Width="520"
        Height="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <TabControl>
            <TabItem Header="Добавить">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8"
                                Orientation="Vertical">
                        <TextBlock Text="Операция: Добавить новый товар"
                                   FontWeight="Bold" />
                        <TextBlock Text="• Название: только буквы и пробелы, поле не должно быть пустым."
                                   Margin="0,6,0,0" />
                        <TextBlock Text="• Количество: целое число > 0." />
                        <TextBlock Text="• Если товар уже существует на выбранном складе — его количество увеличится."
                                   Margin="0,6,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Отгрузить">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Операция: Отгрузить товар"
                                   FontWeight="Bold" />
                        <TextBlock Text="• Название: только буквы и пробелы."
                                   Margin="0,6,0,0" />
                        <TextBlock Text="• Количество: целое число > 0 и не больше остатка." />
                        <TextBlock Text="• Если после отгрузки остаток стал 0, позиция удаляется из списка товаров."
                                   Margin="0,6,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Переместить">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Операция: Переместить товар между складами"
                                   FontWeight="Bold" />
                        <TextBlock Text="• Выберите склад-источник и склад-получатель (они должны отличаться)."
                                   Margin="0,6,0,0" />
                        <TextBlock Text="• Выберите товар со склада-источника (список содержит только товары с остатком &gt; 0)." />
                        <TextBlock Text="• Количество: целое число > 0 и не больше остатка на складе-источнике." />
                        <TextBlock Text="• При перемещении остаток на источнике уменьшается (если станет 0 — позиция удаляется), на получателе увеличивается."
                                   Margin="0,6,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Подсказки">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <TextBlock Text="Подсказки"
                                   FontWeight="Bold" />
                        <TextBlock Text="• Валидация ввода: название — только буквы/пробелы; количество — только цифры."
                                   Margin="0,6,0,0" />
                        <TextBlock Text="• Индикатор Pre — зелёный, когда все предусловия операции выполнены." />
                        <TextBlock Text="• Индикатор Post — зелёный, когда операция завершилась успешно." />
                        <TextBlock Text="• Список слева всегда показывает товары выбранного склада." />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
